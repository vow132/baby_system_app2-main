# 实习总结

## 项目背景

智能婴儿床监护系统（Baby Bed Monitor）是一款基于 UniApp + Vue 3 + TypeScript 的微信小程序，面向家庭用户提供婴儿实时监护、哭声检测、AI 陪伴、成长记录等功能。后端使用 FastAPI + MySQL。

## 我的职责

负责小程序前端开发，独立完成**微信消息推送模块**的需求分析、方案设计、编码实现和联调测试。

## 主要工作

### 1. 微信消息推送功能（核心成果）

**需求：** 婴儿哭了自动推送到家人微信、每周/每月成长小结推送、小事动态推送。

**我的实现：**

- 设计前端推送架构，确定「前端负责订阅授权 + 后端负责触发发送」的分工方案
- 编写 `subscribe.ts` 工具模块，封装 `uni.requestSubscribeMessage` 订阅授权逻辑
- 在引导页完成页新增「开启消息通知」卡片，引导用户首次使用时订阅模板消息
- 改造设置页「消息通知」开关，对接后端 `GET/PUT /push/settings` 接口，实现开关状态前后端同步
- 新增 `getPushSettings()` API 封装，完善 API 层

**修复的 Bug 共 6 个：**

| Bug | 修复方案 |
|-----|---------|
| 开关默认值写死 true | 改为读取本地订阅状态 |
| 引导页拒绝后卡片消失无法重试 | 改为保留卡片，可再次点击 |
| 清缓存导致订阅标记丢失 | 备份恢复 |
| 空字符串传后端 422 | 改为 undefined |
| 一次性订阅用完不再弹窗 | 每次开开关重新弹授权 |
| 开关只读本地不读后端 | onShow 同步后端 channel_app |

**技术栈：** Vue 3、TypeScript、UniApp、Pinia、uView Plus

### 2. 其他前端工作

- 阅读并理解 6000+ 行已有项目代码
- 分析后端 API 结构和返回格式，确保前端调用对齐
- 与后端沟通接口协议，输出对接文档
- 维护更新日志，记录每次版本变更

## 技术收获

| 技能 | 说明 |
|------|------|
| 微信小程序开发 | 掌握小程序生命周期、原生 API 调用（`uni.requestSubscribeMessage`） |
| UniApp 跨端开发 | 理解编译到 mp-weixin 的流程 |
| Vue 3 组合式 API | `ref`、`computed`、`reactive`、`onShow` |
| TypeScript | 接口类型定义、泛型函数 |
| 前后端联调 | 分析后端接口文档，对齐请求/响应格式 |
| 沟通协作 | 独立输出接口文档，推动跨团队协作 |

## 项目成果

- 完成微信消息推送从 0 到 1 的前端实现
- 梳理了「前端订阅 → 后端触发 → 微信推送」的完整链路
- 输出的更新日志和后端对接清单，方便后续开发接手
